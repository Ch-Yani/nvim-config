# introduction
a  sample easy  small nvim configure 

# nvim-config
1. sudo snap install nvim --classic ;sudo apt-get install ctags fzf;
2. cp all this document to ~/.config/nvim/ (if not exit , create it); 
3. run nvim;
4. nvim will install and loading; wait and enjoy it;
5. modified ./.bashrc; use vim to start nvim; 
	#add setting in EOF
	alias vim="nvim" 
